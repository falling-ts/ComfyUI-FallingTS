# comfyui-mcp 配置

MCP 服务器基于 `IO-AtelierTech/comfyui-mcp`, 通过 stdio 协议与 Claude Code 通信。

## 使用

MCP 由 Claude Code 自动启动, 无需手动运行。

## 端口

与 ComfyUI Desktop 通过 `localhost:8188` 通信。

## 侧边面板

MCP 自动安装了 `comfyui-agent-panel` 侧边面板, 重启 ComfyUI Desktop 后生效。
